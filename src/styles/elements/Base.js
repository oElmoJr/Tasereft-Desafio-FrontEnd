import { createGlobalStyle } from "styled-components";

const Base = createGlobalStyle`

    body {
        background-color: #3B46A7;
        font-family: 'Roboto', sans-serif;
    }
`;

export default Base;